#include <stdio.h>
#include <assert.h>
#include <stdlib.h>

struct AVLTree {
    TYPE size;
    struct AVLNode *root;
};

struct AVLNode {

};

struct
void initAVLTree(struct AVLTree *tree);

int main() {


    return 0;
}

void initAVLTree(struct AVLTree *tree) {

}